// tailwind.config.js
module.exports = {
    darkMode: 'class', // Use class-based dark mode rather than media query.
    content: [
        "./src/**/*.{js,jsx,ts,tsx}", // Adjust the path based on your project's structure.
    ],
    theme: {
        extend: {
            // You can add custom colors, fonts, etc. here.
        },
    },
    plugins: [],
};
